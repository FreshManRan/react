//create 17,07,06 lc
import * as actions from './actions';
export default (state={},action) => {
    let nextState = Object.assign({},state);
    switch(action.type){
        
    }
    return nextState;
}